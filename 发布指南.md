# 发布指南：用 GitHub Actions 自动构建 macOS dmg 与 Windows 安装包

## 1. 这是什么

**GitHub Actions** 是 GitHub 自带的自动化 / CI 服务，不需要额外注册或安装。原理很简单：

1. 仓库里放一个 YAML 文件（本项目是 [`.github/workflows/release.yml`](./.github/workflows/release.yml)），声明「什么时候触发」和「触发后做哪些步骤」。
2. 触发后，GitHub 在它自己托管的虚拟机（称为 runner）上执行这些步骤。runner 有 `ubuntu-latest`、`windows-latest`、`macos-latest`（Apple Silicon）等系统可选。
3. 步骤里可以运行任意命令，也可以复用别人写好的「action」（例如安装 Node、安装 Rust、构建 Tauri 应用）。
4. 每一步的日志、成功失败状态都显示在仓库的 **Actions** 标签页里。

对本项目来说它解决的正是「在哪里打包」的问题：

| 产物 | 只能在哪构建 | 工作流里用的 runner |
|---|---|---|
| macOS `.dmg` | macOS（需要 Xcode 命令行工具与 `hdiutil`） | `macos-latest` |
| Windows `-setup.exe`（NSIS）与 `.msi` | Windows（需要 MSVC 工具链） | `windows-latest` |

以前你在自己的 Mac 上手工打 dmg，Intel 版和 Windows 版都没法提供；现在两台 GitHub 的机器同时构建，几分钟后安装包自动出现在 [Releases](https://github.com/XZL-CODE/cc-history-viewer/releases)。

**费用**：公开仓库使用 GitHub 托管 runner 完全免费，没有分钟数限制。私有仓库有每月免费额度（Free 计划 2,000 分钟），并且 macOS 按 10 倍、Windows 按 2 倍计费，一次约 10 分钟的 macOS 构建会消耗约 100 分钟额度。本仓库是公开的，不用考虑这一点。

**权限**：工作流运行时 GitHub 自动注入一个只对本仓库有效、运行结束即失效的 `GITHUB_TOKEN`。文件里声明了 `permissions: contents: write`，这是创建 Release 和上传附件所需的最小权限。不需要配置任何密钥。

## 2. 本项目的工作流做了什么

`release.yml` 定义了一个名为 **Release desktop builds** 的工作流，包含一个矩阵任务，会在两台机器上各跑一遍：

| 步骤 | 作用 |
|---|---|
| `actions/checkout` | 拉取当前提交的源码 |
| `pnpm/action-setup` + `actions/setup-node` | 安装 pnpm 10 与 Node 22，并缓存依赖 |
| `dtolnay/rust-toolchain` | 安装 stable Rust；macOS 上同时装 `aarch64-apple-darwin` 与 `x86_64-apple-darwin` 两个目标 |
| `swatinem/rust-cache` | 缓存 Rust 编译产物，第二次起构建明显更快 |
| `pnpm install --frozen-lockfile` | 安装前端依赖 |
| `tauri-apps/tauri-action` | 执行 `pnpm tauri build`（内部先跑 `pnpm build`），然后创建或复用 Release 并上传安装包 |

`tauri-action` 会读取 `src-tauri/tauri.conf.json` 里的版本号，把 `v__VERSION__` 替换成例如 `v0.7.0`，用它作为 Release 的标签；标签不存在时会以当前提交创建。Release 标题为「Coding Agent History Viewer v0.7.0」，正文由两部分拼成：[`.github/release-notes.md`](./.github/release-notes.md) 里的本版更新内容，加上工作流 `INSTALL_NOTES` 里固定的安装说明（未签名应用的打开方法）。**每次发版前先更新 release-notes.md**，否则 Release 会带着上一版的更新内容；安装说明不用每次改。

产物：

| 文件 | 说明 |
|---|---|
| `Coding-Agent-History-Viewer_0.7.0_universal.dmg` | macOS 通用二进制，Apple Silicon 与 Intel 都能用（`--target universal-apple-darwin`） |
| `Coding-Agent-History-Viewer_0.7.0_x64-setup.exe` | Windows NSIS 安装包，推荐给普通用户 |
| `Coding-Agent-History-Viewer_0.7.0_x64_en-US.msi` | Windows MSI 安装包，适合企业分发 |

GitHub 会在每个附件旁显示 SHA-256，可用于校验下载。

## 3. 三种触发方式

工作流文件里声明了三种触发条件，任选其一：

### 3.1 推送版本标签（推荐的正式发布方式）

```bash
# 1. 三处版本号改成新版本：package.json、src-tauri/tauri.conf.json、src-tauri/Cargo.toml
#    （在 src-tauri 下跑一次 cargo check 让 Cargo.lock 跟上）
# 2. 在 .github/release-notes.md 写明本版更新内容
# 3. 提交并合并到 main
# 4. 在 main 上打标签并推送
git tag v0.8.0
git push origin v0.8.0
```

推送标签后约 1 分钟内 Actions 页面会出现新的运行，macOS 与 Windows 两个任务并行，首次约 10–15 分钟（有缓存后更快）。结束后 Releases 页面就有对应版本。

### 3.2 改动触发文件（从任意分支发起一次构建）

改动 [`.github/release-trigger`](./.github/release-trigger)（例如把序号加一）并推送到任意分支，也会触发构建。这是为「不想先打标签、只想先出一版安装包试试」准备的：Release 标签仍取 `tauri.conf.json` 的版本号，指向被推送的那个提交。本次 v0.7.0 就是用这种方式从 PR 分支发起的。

> 如果之后只想保留标签触发，把 `release.yml` 里 `push.branches` 与 `push.paths` 两行删掉即可。

### 3.3 在网页上手动运行

工作流合并进 `main` 之后，仓库的 **Actions → Release desktop builds** 页面右侧会出现 **Run workflow** 按钮，选择分支点击即可。工作流文件只在其他分支上时，这个按钮不会出现。

## 4. 如何查看进度和排错

1. 打开仓库的 **Actions** 标签页，点开对应的运行，能看到 macOS 与 Windows 两个任务的实时日志。
2. 某一步失败时，展开红色的步骤看报错；常见原因是依赖锁文件与 `package.json` 不一致（`--frozen-lockfile` 会拒绝）、Rust 编译错误、或 Tauri 打包配置问题。
3. 修复后重新推送标签（先 `git push --delete origin v0.8.0`，再重新打标签推送）或再改一次触发文件；也可以在运行页面点 **Re-run failed jobs** 只重跑失败的任务。
4. 同一版本重复构建时，`tauri-action` 会复用已存在的 Release 并覆盖同名附件。

## 5. 签名与公证（可选）

当前产物**未签名**：

- macOS 首次打开会提示「已损坏」或「无法验证开发者」。用户可在终端执行 `xattr -cr "/Applications/Coding Agent History Viewer.app"`，或在 Finder 里右键「打开」。
- Windows 会弹出 SmartScreen 提示，选择「更多信息 → 仍要运行」。

要去掉这些提示需要付费证书。有 Apple Developer 账号（99 美元/年）后，在仓库 **Settings → Secrets and variables → Actions** 里添加以下密钥，`tauri-action` 会自动签名并公证：

| Secret | 内容 |
|---|---|
| `APPLE_CERTIFICATE` | Developer ID Application 证书导出的 `.p12`，base64 编码 |
| `APPLE_CERTIFICATE_PASSWORD` | 导出 `.p12` 时设置的密码 |
| `APPLE_SIGNING_IDENTITY` | 证书名称，如 `Developer ID Application: 你的名字 (TEAMID)` |
| `APPLE_ID` / `APPLE_PASSWORD` / `APPLE_TEAM_ID` | 公证用的 Apple ID、App 专用密码与团队 ID |

然后在 `release.yml` 的 `tauri-action` 步骤 `env` 里把这些 secrets 传入即可（Tauri 官方文档 “macOS Code Signing” 有逐项说明）。Windows 代码签名证书同理，通常成本更高，个人项目可以不做。

## 6. 之后可以扩展的点

- **Linux**：矩阵里加一个 `ubuntu-22.04` 任务并先 `apt-get install` WebKitGTK 等依赖，即可产出 `.AppImage` 与 `.deb`。
- **自动更新**：接入 `tauri-plugin-updater` 并生成 `latest.json`，应用内可检查新版本；需要一对更新签名密钥。
- **在 PR 上跑测试**：再加一个只运行 `cargo test`、`tsc`、`pnpm build` 的工作流，每个 PR 自动验证。

## 7. 本次（v0.7.0）的情况

- 工作流文件与触发文件在 PR [#1](https://github.com/XZL-CODE/cc-history-viewer/pull/1) 的分支上，首次运行由改动触发文件发起，运行记录见 Actions 页面。
- 这个开发会话的 git 代理只允许推送指定分支，不允许推标签，GitHub 集成也没有 Actions 的写权限，所以没有用「推标签」和「手动运行」两种方式。合并 PR 之后，这两种方式你在本机或网页上都能直接用。
